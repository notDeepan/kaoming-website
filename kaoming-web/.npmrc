# @react-three/fiber declares an OPTIONAL peer on expo, for React Native. npm 10
# tries to satisfy it anyway and then fails resolving expo's own peers, so a
# plain `npm install` cannot complete. This project is web-only and never loads
# that path.
legacy-peer-deps=true
